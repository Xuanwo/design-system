Checkbox with an optional label and description line. Supports the indeterminate (mixed) state for "select all".

```jsx
<Checkbox label="Subscribe to the newsletter" defaultChecked />
<Checkbox label="Select all" indeterminate onChange={…} />
<Checkbox label="Public" description="Anyone with the link can read." />
```
